rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 🏠 กฎสำหรับ collection reports 
    match /reports/{reportId} {
      allow read: if true;
      allow write: if request.auth != null;
      
      // 💬 Subcollection comments ภายใน reports เท่านั้น
      match /comments/{commentId} {
        allow read: if true;
        allow write: if request.auth != null;
      }
    }
    
    // 📷 กฎสำหรับ camera reports - ระบบรายงานกล้องใหม่ (ปรับปรุงใหม่)
    match /camera_reports/{reportId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (ไม่ต้องล็อกอิน)
      allow create: if request.auth != null;
      allow update: if request.auth != null;  // เปิดให้อัปเดตได้ทั้งหมดสำหรับผู้ล็อกอิน
      allow delete: if request.auth != null && (
        // เจ้าของสามารถลบได้ถ้า status เป็น pending
        (request.auth.uid == resource.data.reportedBy && resource.data.status == 'pending') ||
        // ระบบสามารถลบรายงาน verified/rejected ได้ (สำหรับ auto-removal)
        (resource.data.status == 'verified' || resource.data.status == 'rejected')
      );
    }
    
    // 🗳️ กฎสำหรับ camera votes - โหวตสำหรับกล้อง (ปรับปรุงใหม่)
    match /camera_votes/{voteId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน
      allow create: if request.auth != null;  // ผู้ล็อกอินสามารถสร้างโหวตได้
      allow update: if request.auth != null;  // เปิดให้อัปเดตได้สำหรับผู้ล็อกอิน
      allow delete: if request.auth != null;  // เปิดให้ลบได้สำหรับผู้ล็อกอิน
    }
    
    // 📈 กฎสำหรับ user report stats - สถิติการรายงาน
    match /user_report_stats/{userId} {
      allow read: if request.auth != null;  // ให้อ่านได้ทั้งหมด
      allow write: if request.auth != null;  // ให้เขียนได้สำหรับการอัปเดต stats
    }
    
    // 🎯 กฎสำหรับ speed cameras - กล้องจับความเร็วหลัก (อัปเดตใหม่)
    match /speed_cameras/{cameraId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน
      allow create: if request.auth != null;  // ผู้ล็อกอินสามารถสร้างกล้องใหม่ได้ (จากระบบ auto-verification)
      allow update: if request.auth != null;  // อัปเดตได้สำหรับผู้ล็อกอิน
      allow delete: if request.auth != null && (
        // อนุญาตให้ลบโดยระบบหรือ admin
        request.auth.token.isAdmin == true ||
        // หรือเมื่อมีฟิลด์ markedForDeletion เป็น true (จากระบบ auto-deletion)
        resource.data.markedForDeletion == true ||
        // หรือผู้ใช้ล็อกอินปกติ (สำหรับการลบด้วยมือ)
        request.auth != null
      );
    }
    
    // ✅ กฎสำหรับ camera promotion success - บันทึกการสร้างกล้องสำเร็จ
    match /camera_promotion_success/{logId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // ผู้ล็อกอินสามารถสร้าง success log ได้
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ success log
    }
    
    // ❌ กฎสำหรับ camera promotion errors - บันทึกข้อผิดพลาดการสร้างกล้อง
    match /camera_promotion_errors/{errorId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // ผู้ล็อกอินสามารถสร้าง error log ได้
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ error log
    }
    
    // 📝 กฎสำหรับ camera deletion log - ประวัติการลบกล้อง
    match /camera_deletion_log/{logId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // ผู้ล็อกอินสามารถสร้าง log ได้
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ log
    }
    
    // ❌ กฎสำหรับ camera deletion errors - ประวัติ errors การลบกล้อง
    match /camera_deletion_errors/{errorId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // ผู้ล็อกอินสามารถสร้าง error log ได้
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ error log
    }
    
    // 👤 กฎสำหรับข้อมูลผู้ใช้ - เฉพาะเจ้าของ
    match /users/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 📊 กฎสำหรับจำกัดการโพสต์ - เฉพาะเจ้าของ
    match /userLimits/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 📈 กฎสำหรับสถิติผู้ใช้ - เฉพาะเจ้าของ
    match /user_stats/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 🔑 FCM Tokens - เฉพาะเจ้าของ
    match /user_tokens/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // 🏃 กฎสำหรับ speed limit changes - ประวัติการเปลี่ยนความเร็ว
    match /speed_limit_changes/{changeId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update: if false;  // ไม่อนุญาตให้แก้ไข
      allow delete: if request.auth != null;  // อนุญาตให้ลบได้ (สำหรับ camera removal)
    }
    
    // ❌ กฎสำหรับ speed update errors - ข้อผิดพลาดการอัปเดตความเร็ว
    match /speed_update_errors/{errorId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ
    }
    
    // 🔔 Notification Posts - อ่านได้ทุกคน สร้างได้เฉพาะ login
    match /notification_posts/{postId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if false;
    }
    
    // 📊 Notification Stats - อ่านได้เฉพาะ login, เขียนไม่ได้
    match /notification_stats/{statId} {
      allow read: if request.auth != null;
      allow create, update, delete: if false;
    }
    
    // ✅ กฎสำหรับ verified reports removal log - บันทึกการลบรายงานที่ยืนยันแล้ว
    match /verified_reports_removal_log/{logId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ auto-removal
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ log
    }
    
    // 🗑️ กฎสำหรับ camera verifications - การยืนยันกล้อง
    match /camera_verifications/{verificationId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update: if request.auth != null;  // อัปเดตได้เฉพาะระบบ
      allow delete: if request.auth != null;  // ลบได้เฉพาะระบบ (สำหรับ complete removal)
    }
    
    // 📊 กฎสำหรับ camera statistics - สถิติการใช้งานกล้อง
    match /camera_statistics/{statisticId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update: if request.auth != null;  // อัปเดตได้เฉพาะระบบ
      allow delete: if request.auth != null;  // ลบได้เฉพาะระบบ (สำหรับ complete removal)
    }
    
    // 📦 กฎสำหรับ archived camera reports - รายงานที่ถูกเก็บถาวร
    match /archived_camera_reports/{reportId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบข้อมูลเก็บถาวร
    }
    
    // 🗳️ กฎสำหรับ archived camera votes - โหวตที่ถูกเก็บถาวร
    match /archived_camera_votes/{voteId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบข้อมูลเก็บถาวร
    }
    
    // 🗑️ กฎสำหรับ deleted cameras - ประวัติกล้องที่ถูกลบ
    match /deleted_cameras/{cameraId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบประวัติ
    }
    
    // ❌ กฎสำหรับ camera removal failures - ประวัติ errors การลบกล้อง
    match /camera_removal_failures/{errorId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ error log
    }
    
    // 🧹 กฎสำหรับ camera cleanup log - ประวัติการทำความสะอาดข้อมูลกล้อง
    match /camera_cleanup_log/{logId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ cleanup log
    }
    
    // ❌ กฎสำหรับ camera cleanup errors - ประวัติ errors การทำความสะอาดกล้อง
    match /camera_cleanup_errors/{errorId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ error log
    }
    
    // 🔍 กฎสำหรับ orphaned data cleanup log - ประวัติการทำความสะอาดข้อมูลค้าง
    match /orphaned_data_cleanup_log/{logId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ cleanup log
    }
    
    // ❌ กฎสำหรับ orphaned data cleanup errors - ประวัติ errors การทำความสะอาดข้อมูลค้าง
    match /orphaned_data_cleanup_errors/{errorId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ error log
    }
    
    // 🏃 กฎสำหรับ deleted speed limit changes log - ประวัติการลบ speed limit changes
    match /deleted_speed_limit_changes_log/{logId} {
      allow read: if true;  // เปิดให้อ่านได้ทุกคน (สำหรับ debugging)
      allow create: if request.auth != null;  // สร้างได้เฉพาะระบบ
      allow update, delete: if false;  // ไม่อนุญาตให้แก้ไขหรือลบ log
    }
  }
}